main
{
    var{
        integer x;
        integer s;
    }

    //----3.1 & 5.1----
    x = (1 + 3) * 2;
    write(x);

    //----3.2----
    if (x > 7) then {
        write(8);
    }
    else{
        write(0);
    };

    //----3.3----
    x = 0;
    while (x <= 10) {

        //----3.4----
        write(x);
        read(s);
        x = x + 1;
    };
}