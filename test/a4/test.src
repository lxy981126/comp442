class POLYNOMIAL
{
	public func evaluate(float x) : integer;
	public integer i;
};

func POLYNOMIAL::evaluate(float x) : integer
{
    i = 6;
    return (0);
}

main
{
    var{
        integer x;
        POLYNOMIAL p;
    }

    p.i = 5;
    x = p.evaluate(2);
    //write(p.i);
}